import { TemplateReplacementPipe } from './template-replacement.pipe';

describe('TemplateReplacementPipe', () => {
  it('create an instance', () => {
    const pipe = new TemplateReplacementPipe();
    expect(pipe).toBeTruthy();
  });
});
