name: Check CRLF

on: pull_request

jobs:
  check-CRLF:
    name: Check CRLF action
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@main

      - name: check-crlf
        uses: erclu/check-crlf@v1